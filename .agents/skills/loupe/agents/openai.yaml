interface:
  display_name: "Loupe"
  short_description: "Run multiple parallel external code reviews"
  default_prompt: "Use $loupe to review my current uncommitted changes."
policy:
  allow_implicit_invocation: false
