interface:
  display_name: "Loupe"
  short_description: "Parallel external review synthesis"
  default_prompt: "Use $loupe to review my current uncommitted changes."
policy:
  allow_implicit_invocation: false
